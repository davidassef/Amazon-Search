# Example .env configuration for Vite

# Base URL for backend API
# Use a full URL for cross-origin deployment or a relative path for same origin
VITE_API_BASE_URL=/api

# Default UI locale
VITE_DEFAULT_LOCALE=en

